{{ USE cache = Cache( cache => c.config.renderCache ) }}

{{ cache.proc('template' => 'post/list_.tt', 'key' => posts.uid(c.user)) }}

{{ IF c.user.active_artist }}
	{{ INCLUDE post/submit.tt }}
{{ END }}
